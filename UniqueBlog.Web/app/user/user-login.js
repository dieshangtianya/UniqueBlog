require(["./../config"], function () {
	require(["user.login.module"]);
})

define("user.login.module", [
		"jquery"
		, "domready"
        , "jqueryValidate"
		, "jqueryValidateUnobtrusive"
], function ($, domready) {

	domready(function () {

	});
});